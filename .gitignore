# Runtime do Claude Code — não versionar (os symlinks de .claude/skills/ SÃO versionados)
.claude/scheduled_tasks.lock
.claude/settings.local.json

# Skills vão unzipadas no plugin. Zip local fica atrasado da pasta.
skills/*.zip
